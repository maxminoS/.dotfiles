#!/bin/sh

# Aliases
alias vi='vim'

# Verbosity
alias cp='cp -iv'
alias mv='mv -iv'
alias rm='rm -vI'

# Color
alias ls='ls --color=auto'
alias grep='grep --color=auto'

alias updateall='sudo apt update; sudo apt upgrade'
